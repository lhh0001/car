# 覆盖 nav2_params.yaml：Gazebo 使用 /clock。
controller_server:
  ros__parameters:
    use_sim_time: true
local_costmap:
  local_costmap:
    ros__parameters:
      use_sim_time: true
      voxel_layer:
        scan:
          raytrace_max_range: 10.0
          obstacle_max_range: 6.0
global_costmap:
  global_costmap:
    ros__parameters:
      use_sim_time: true
      obstacle_layer:
        scan:
          raytrace_max_range: 10.0
          obstacle_max_range: 6.0
planner_server:
  ros__parameters:
    use_sim_time: true
behavior_server:
  ros__parameters:
    use_sim_time: true
bt_navigator:
  ros__parameters:
    use_sim_time: true
velocity_smoother:
  ros__parameters:
    use_sim_time: true
amcl:
  ros__parameters:
    use_sim_time: true
map_server:
  ros__parameters:
    use_sim_time: true
lifecycle_manager_localization:
  ros__parameters:
    use_sim_time: true
lifecycle_manager_navigation:
  ros__parameters:
    use_sim_time: true
