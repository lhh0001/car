# 覆盖 nav2_params.yaml：真实传感器使用系统时间。
controller_server:
  ros__parameters:
    use_sim_time: false
local_costmap:
  local_costmap:
    ros__parameters:
      use_sim_time: false
global_costmap:
  global_costmap:
    ros__parameters:
      use_sim_time: false
planner_server:
  ros__parameters:
    use_sim_time: false
behavior_server:
  ros__parameters:
    use_sim_time: false
bt_navigator:
  ros__parameters:
    use_sim_time: false
velocity_smoother:
  ros__parameters:
    use_sim_time: false
amcl:
  ros__parameters:
    use_sim_time: false
map_server:
  ros__parameters:
    use_sim_time: false
lifecycle_manager_localization:
  ros__parameters:
    use_sim_time: false
lifecycle_manager_navigation:
  ros__parameters:
    use_sim_time: false
